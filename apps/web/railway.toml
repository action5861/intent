# [배포 #3] Railway 웹 서비스 설정
[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/web/Dockerfile"
dockerContext = "."

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
