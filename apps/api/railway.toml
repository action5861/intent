# [배포 #3] Railway API 서비스 설정
[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"
dockerContext = "."

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
